# OpenAI — the agent's brain. https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Optional: which repository the agent stars (defaults shown).
# TARGET_REPO_OWNER=swytchcode
# TARGET_REPO_NAME=swytchcode-examples

# NOTE: There is deliberately NO GitHub token here.
# Connect GitHub once and Swytchcode injects the credential at runtime:
#   swytchcode auth connect github
