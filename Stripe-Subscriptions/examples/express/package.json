{
  "name": "stripe-subscriptions-example-express",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal Express server wiring the single-file activationService.ts",
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "echo 'no lint configured' && exit 0",
    "test": "echo 'no tests' && exit 0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "swytchcode-runtime": "^0.1.5"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
